# No custom environment variables are required for this applet.
# Default platform environment keys (like GEMINI_API_KEY) are automatically
# provisioned and managed safely by the AI Studio hosting container.

