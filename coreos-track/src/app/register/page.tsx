import { AuthForm } from "@/components/AuthForm";

export const metadata = { title: "Request access · CoreOs Track" };

export default function RegisterPage() {
  return <AuthForm mode="register" />;
}
